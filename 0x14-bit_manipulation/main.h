#ifndef MAIN_H
#define MAIN_H
#include<stdio.h>
#include <stdlib.h>
#include <math.h>

int _putchar(char c);
unsigned int binary_to_uint(const char *b);
int _power(int x, int y);
int _atoi(const char *s);
#endif
